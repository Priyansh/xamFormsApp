<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="xamFormsApp.ListCustomPage"
             Padding="0, 20, 0, 0"
             BackgroundColor="LightBlue">
    <ListView x:Name="lstElement" SeparatorColor="Brown" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <!--<ImageCell ImageSource="{Binding ImageUrls}" Text="{Binding Name}" Detail="{Binding Details}" />-->
                <ViewCell>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Image Source="{Binding ImageUrls}" />
                        <StackLayout HorizontalOptions="StartAndExpand">
                            <Label Text="{Binding Name}" TextColor="BlueViolet" />
                            <Label Text="{Binding Details}" />
                        </StackLayout>
                        <Button Text="Follow" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>